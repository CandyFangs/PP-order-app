<%= form_for([@material, @color]) do |f| %>
	<div class="form-group">
		<%= f.label :color, "Kolor" %>
		<%= f.text_field :color %>
	</div>
	<div class="form-group">
		<%= f.label :picture, "Zdjęcie" %>
		<%= f.file_field :picture %>
	</div>
<br />
<%= f.submit "Zapisz" %>
<% end %>
